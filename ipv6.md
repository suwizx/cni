# ipv6

สำหรับ **Part 6: Basic IPv6 Connectivity** เพื่อให้ได้คะแนนเต็ม 2 คะแนน (ข้อละ 0.5) หัวใจสำคัญคือการทำให้ Router ทุกตัวรู้จักเส้นทาง IPv6 ของกันและกัน (Routing) ครับ

นี่คือ Checklist และวิธีทำแบบ Step-by-Step ครับ:

### ✅ Checklist: IPv6 Connectivity

| **รายการทดสอบ** | **เป้าหมาย (IPv6 Address)** | **สถานะ** |
| --- | --- | --- |
| **ubuntu2** -> **ubuntu1** | `ping6 2001:6707:191:245::2` |  |
| **ubuntu2** -> **Switch** | `ping6 2001:6707:191:245::3` |  |
| **ubuntu1** -> **ubuntu2** | `ping6 2001:6707:191:199:[เลข SLAAC ของคุณ]` |  |
| **ubuntu2** -> **Router1** | `ping6 2001:6707:191:1::1` |  |

---

### 🛠️ วิธีทำให้ปิงเจอ (Step-by-Step)

สาเหตุหลักที่ตอนนี้อาจจะยังปิงข้ามไปหา Router 1 หรือข้าม VLAN ไม่เจอ เพราะ **Router ยังไม่มีเส้นทาง (Routing Table)** ครับ

### 1. เปิดการ Routing บน Router ทุกตัว (สำคัญมาก)

ต้องมั่นใจว่าทั้ง Router 1 และ Router 2 รันคำสั่งนี้:

Bash

`conf t
ipv6 unicast-routing`

### 2. ตั้ง Static Route บน Router 1 (เพื่อให้รู้จักทางไป VLAN A และ B)

Router 1 ต้องรู้ว่าถ้าจะไปหาเครื่องลูก (245 และ 199) ต้องส่งมาที่ Router 2:

Bash

`! บน Router 1
conf t
ipv6 route 2001:6707:191:245::/64 2001:6707:191:1::2
ipv6 route 2001:6707:191:199::/64 2001:6707:191:1::2`

### 3. ตั้ง Default Route บน Router 2 (เพื่อให้รู้จักทางไปหา Router 1)

Router 2 ต้องรู้ว่าถ้าจะไป Subnet อื่นๆ (หรือไปหา Router 1) ให้ส่งออกทางขาเชื่อมต่อ:

Bash

`! บน Router 2
conf t
ipv6 route ::/0 2001:6707:191:1::1`

### 4. ตรวจสอบ Gateway บน Ubuntu และ Switch

ต้องมั่นใจว่าทุกเครื่องชี้ Gateway มาที่ Router 2 (Link-Local):

- **Ubuntu 1:** ใน Netplan ต้องมี `via: "fe80::1"` ใต้ส่วน IPv6
- **Ubuntu 2:** ตรวจสอบว่าได้รับ Gateway จาก SLAAC หรือยัง (`ip -6 route`)
- **Switch:** ต้องมีคำสั่ง `ipv6 default-gateway fe80::1` (ใน VLAN A)

---

### 🧪 วิธีทดสอบการปิง (ใช้ ping6)

**จาก ubuntu2 ไปหา ubuntu1:**

Bash

`ping6 2001:6707:191:245::2 -c 4`

**จาก ubuntu2 ไปหา Switch:**

Bash

`ping6 2001:6707:191:245::3 -c 4`

**จาก ubuntu2 ไปหา Router 1 (ขาที่เชื่อมกับ R2):**

Bash

`ping6 2001:6707:191:1::1 -c 4`

---

### 💡 เทคนิคแก้ปัญหา (ถ้ายังปิงไม่เจอ)

1. **Check Neighbor:** บน Router ลองพิมพ์ `show ipv6 neighbors` เพื่อดูว่ามันเห็นเครื่องลูกในแลนไหม
2. **Check Route:** บน Router ลองพิมพ์ `show ipv6 route` เพื่อดูว่ามีตัวอักษร **S** (Static) ของ Subnet เป้าหมายปรากฏอยู่หรือไม่
3. **Firewall:** บน Ubuntu มั่นใจว่าไม่ได้เปิด `ufw` ไว้ (ใช้ `sudo ufw disable`)

ถ้าตั้ง Static Route ตามข้อ 2 และ 3 เสร็จแล้ว ลองปิงดูอีกรอบ คะแนน 2.0 เต็มก็น่าจะมาทันทีครับ! ถ้าติดข้อไหนแจ้งได้เลยครับ